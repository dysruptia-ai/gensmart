Claude mira el resultado de las pruebas:

### 1.3 Sesión y Tokens

-Estando en /dashboard, F5 (refresh) → sesión se mantiene ✅
-DevTools > Application > Local Storage → NO hay access token✅
-DevTools > Application > Cookies → existe cookie refresh_token (httpOnly)✅
-Abrir pestaña incógnito → ir a /dashboard → redirige a /login✅
### 1.4 Logout
-Click en avatar/dropdown en header → opción Logout visible✅
-Click Logout → redirige a /login✅
-Tras logout, navegar manualmente a /dashboard → redirige a /login✅
-Cookie refresh_token ya no existe en DevTools✅
### 1.5 Forgot / Reset Password
-/forgot-password → formulario carga con campo email✅
-Submit con email → no crashea (OK si SMTP no configurado)✅
-/reset-password/fake-token → página carga (formulario visible)✅
### 1.6 Dashboard Layout
-Sidebar visible con iconos lucide-react✅
-Sidebar links: Agents, Conversations, Contacts, Funnel, Calendar, Billing, Settings✅
-Header muestra avatar/nombre del usuario✅
-Header dropdown: opciones visibles (Settings, Logout)✅
-Logo GenSmart en sidebar✅
-Sidebar responsive: se colapsa en viewport < 768px✅(ver imagen 1)
### 1.7 Settings — General (/dashboard/settings)
-Página settings carga con sidebar de navegación (General, Team, Sub-accounts, Security, Data)✅
-General: muestra org name actual✅
-Editar org name → Save → recargar → muestra nuevo nombre✅
-Timezone selector visible y funcional✅
-Language selector visible✅
### 1.8 Settings — Team (/dashboard/settings/team)
-Lista miembros muestra al usuario actual con su rol✅
-Botón "Invite Member" abre modal✅
-Modal invite: campo email + role selector✅
-Submit invite → no crashea (OK si no envía email real)✅, cada ves que digito una letra en el campo email,pierde el foco✅(mal UX)
### 1.9 Settings — Sub-accounts (/dashboard/settings/sub-accounts)
-Página carga (puede estar vacía si plan Free no permite sub-accounts)✅
-Si hay botón "Create": click → formulario aparece✅
-Si plan Free bloquea → muestra mensaje de upgrade✅
### 1.10 Settings — Security / 2FA (/dashboard/settings/security)
-Página muestra estado 2FA: "Disabled"✅
-Click "Enable 2FA" → muestra QR code✅
-Escanear QR con Google Authenticator / Authy✅
-Ingresar código TOTP → 2FA se habilita → muestra backup codes✅
-Copiar/anotar al menos 1 backup code✅
-Logout → Login → ahora pide código TOTP✅
-Ingresar código TOTP correcto → accede al dashboard✅
-Logout → Login → usar backup code en vez de TOTP → accede✅
-Settings > Security → Disable 2FA → confirmar password → estado vuelve a "Disabled"✅ -> muestra como si no hubiese activado 2FA(imagen 2) -> No deja deshabilitar 2FA
### 1.11 Consola y Visual ✅
---
## FASE 2 — Landing Page + Pricing + Blog
### 2.1 Layout Público✅
-/docs/whatsapp-setup -> no tiene pagina placeholder
-Mobile (375px): hamburger menu aparece, slide-in panel funciona✅, El logo del footer darle un poco mas de tamaño,porque se ve mal no se puede leer✅

### 2.2 Landing — Secciones (verificar que cada sección existe y se ve bien)✅

### 2.3 Landing — Interactividad✅
- Cuando estoy en /pricing y quiero ir a "features", no muestra nada✅. Desde la url / -> home si funciona -> sale este warning en dev tools:
Blocked aria-hidden on an element because its descendant retained focus. The focus must not be hidden from assistive technology users. Avoid using aria-hidden on a focused element or its ancestor. Consider using the inert attribute instead, which will also prevent focus. For more details, see the aria-hidden section of the WAI-ARIA specification at https://w3c.github.io/aria/#aria-hidden.
Element with focus: <button.PublicNavbar-module__bDS2Xq__mobileLink>
Ancestor with aria-hidden: <div.PublicNavbar-module__bDS2Xq__mobileMenu > 
---

### 2.4 Landing — Mockups CSS✅

### 2.5 Landing — Mobile Responsive✅

### 2.6 Pricing Page (/pricing)✅

### 2.7 Blog (/blog)✅

### 2.8 BlogPreview en Landing✅

### 2.9 SEO✅

### 2.10 Calidad General Fase 2✅
-BUG cuando me autentico y estoy en /dashboard -> navego hacia / -> landing, sigue mostrando los botones para autenticarme y registrarme✅. deberia mostrar un solo boton para poder ir al dashboard.

